import router from '@/console/ssl-certificate/router'
import config from '@/console/ssl-certificate/config'
import zh from '@/console/ssl-certificate/locale/zh'

export default {
  router,
  config,
  locale: {
    zh: zh.default
  }
}
